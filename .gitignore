# App artifacts
/_build
/db
/deps
/*.ez
/rel
*.pyc

# Generate on crash by the VM
erl_crash.dump
.DS_Store

# Static artifacts
/node_modules
/bower_components

# Since we are building assets from web/static,
# we ignore priv/static. You may want to comment
# this depending on your deployment strategy.
/priv/static/
/priv/gettext

# The config/prod.secret.exs file by default contains sensitive
# data and you should not commit it into version control.
#
# Alternatively, you may comment the line below and commit the
# secrets file as long as you replace its contents by environment
# variables.
/config/prod.secret.exs
/config/dev.secret.exs
/config/myhost
/config/consts.exs
/config/translate_filters.yml
/config/translate_filters.exs
/config/exblur.conf
/config/exblur.schema.exs

tags
TODO.secret
/uploads

/.bundle/
/.byebug_history
/web/models/scrapy.ex
/web/models/site.ex
/Gemfile
/Gemfile.lock
/vendor/
/.hg
/.hgignore

/do
/prod-do
/build
/deploy
/deploy-production

/exblur.tar.gz

/web/templates
/web/static/js/ads.js
/web/static/js/dmm.js
